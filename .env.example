# --- Gmail (Google Cloud OAuth client, "Desktop app" type) ---
GMAIL_CLIENT_ID=
GMAIL_CLIENT_SECRET=
# Obtained by running `npm run gmail:auth` once.
GMAIL_REFRESH_TOKEN=
# Gmail search query used to find candidate emails each run.
GMAIL_QUERY=in:inbox -label:bill-processed -label:bill-needs-review newer_than:30d
GMAIL_PROCESSED_LABEL=bill-processed
GMAIL_NEEDS_REVIEW_LABEL=bill-needs-review

# --- QuickBooks Online (Intuit developer app) ---
QBO_CLIENT_ID=
QBO_CLIENT_SECRET=
# "sandbox" while testing, "production" when ready to write real bills.
QBO_ENVIRONMENT=sandbox
QBO_REDIRECT_URI=http://localhost:3000/oauth2callback
# Fallback expense account (by name, exact match) used when Claude's
# suggested category doesn't match an existing QBO account.
QBO_DEFAULT_EXPENSE_ACCOUNT=Utilities

# --- Claude (Anthropic API) ---
ANTHROPIC_API_KEY=
CLAUDE_MODEL=claude-sonnet-5

# --- Behavior ---
# Bills extracted with confidence below this are NOT auto-entered into
# QuickBooks; they're labeled bill-needs-review for you to handle by hand.
CONFIDENCE_THRESHOLD=0.75
# Where local state (processed-message ledger) is stored.
DB_PATH=./data/state.sqlite3
